export type Champion = {
    id: string;
    name: string;
    baseAD: number;
    baseAS: number;
    critChance: number;
  };
  
  export type Item = {
    id: string;
    name: string;
    effect: string;
  };
  